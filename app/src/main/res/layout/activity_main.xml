<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">


    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/grid_layout_manager"
            style="@style/ButtonSelect"
            android:text="Grid Layout Manager"
            android:textColor="@android:color/white" />

        <TextView
            android:id="@+id/variable_span_size"
            style="@style/ButtonSelect"
            android:text="Variable span size"
            android:textColor="@android:color/white" />

        <TextView
            android:id="@+id/header"
            style="@style/ButtonSelect"
            android:text="Header"
            android:textColor="@android:color/white" />

        <TextView
            android:id="@+id/autofit"
            style="@style/ButtonSelect"
            android:text="Autofit RecyclerView"
            android:textColor="@android:color/white" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>